version: '2.3'

services:
  gcvit:
    build:
      context: .
      # change target to "api" to build/test api server only
      target: api-ui
    ports:
      - "8080:8080"
    volumes:
      - ./server/assets:/app/assets:ro
      - ./server/config:/app/config:ro
