version: '2.3'

services:
  gcvit:
    build:
      context: .
#     target: api
    ports:
      - "8080:8080"
    volumes:
      - ./server/assets:/app/assets:ro
      - ./server/config:/app/config:ro
